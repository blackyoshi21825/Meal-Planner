{
    "name": "cora-meal-planner",
    "version": "1.0.0",
    "private": true,
    "description": "CORA - Meal Planner PWA prepared for Android via Capacitor",
    "scripts": {
        "prepare-android": "npx cap sync android",
        "open-android": "npx cap open android",
        "copy-web-assets": "npx cap copy",
        "build-apk-debug": "cd android && .\\gradlew.bat assembleDebug"
    },
    "dependencies": {
        "@capacitor/core": "^5.0.0"
    },
    "devDependencies": {
        "@capacitor/cli": "^5.0.0"
    }
}